# **DesafioCiencia-de-dados**

Um pequeno exemplo de analise de dados / A small exemple of data analysis

## English:

This is a data analysis project I made using Python for my Data Science Class. It uses Python notebooks. It analyzes a public dataset avaliable through the first link in the notebook. The project is separeted in 2 parts: first I clean the data and analyze it manually an then I create an AI system to try and predict the data.

Running the code is easy
1. Simply open the .ipynb file using Jupiter, Google Collab or any other program that can open Python notebooks
2. Insert the CSV  contaning the dataset file in your environment. The specific place the file should be is in the line " insurence_data = pd.read_csv("/content/insurance.csv") "
3. Run the code cells in order

It's important to note that this notebook uses multiple third-party libraries that you might have to install such as tensorflow, Keras and seaborn

## Português:

Esse é um projeto de analise de dados que eu fiz usando Python para minha cadeira de Ciência de dados. Ele utiliza notebooks de Python. Ele analiza um dataset publico, disponivel através do primeiro link no notebook. O projeo é separado em 2 partes: Primeiro eu limpo os dados e os analizo manualmente e depois eu crio um sistema de IA para prever os dados. 

Rodar o código é facil
1. Simplesmente abra o arquivo .ipynb usando Jupiter, Google Collab or qualquer outro programa que possa abrir notebook Python
2. Insira o arquivo CSV mp seu ambiente. O lugar especifico que o arquivo deve ficar está na linha " insurence_data = pd.read_csv("/content/insurance.csv") "
3. Rode as celulas de código em ordem

É importante resaltar que esse notebook usa multplas bibliotecas de terceiros que talvez você precise instalar como tensorflow, Keras e seaborn
